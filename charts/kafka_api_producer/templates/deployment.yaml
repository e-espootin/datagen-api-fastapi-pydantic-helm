apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.appName }}
    # Add recommended Kubernetes labels
    app.kubernetes.io/name: {{ .Values.appName }}
    app.kubernetes.io/version: {{ .Values.image.tag }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.appName }}
      tier: frontend
  template:
    metadata:
      labels:
        app: {{ .Values.appName }}
        tier: frontend
    spec:
      containers:
      - name: {{ .Values.container_name }}
        image: "{{ .Values.image.name }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
        env:
        - name: DSNAME
          value: "{{ .Values.env.DSNAME }}"
        - name: SLEEP_TIME_SEC
          value: "{{ .Values.env.SLEEP_TIME_SEC }}"
        - name: ALL_DATASETS
          valueFrom:
            configMapKeyRef:
              name: my-env-configmap
              key: ALL_DATASETS
        - name: API_LOCAL_URL
          valueFrom:
            configMapKeyRef:
              name: my-env-configmap
              key: API_LOCAL_URL
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: my-env-configmap
              key: KAFKA_BOOTSTRAP_SERVERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        # Add healthcheck
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "echo 'healthy' && exit 0"  # Always returns exit code 0
          initialDelaySeconds: 10
          periodSeconds: 10